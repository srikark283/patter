import HUD from "./HUD";
import "./index.css";

function HudApp() {
  return (
    <main className="w-screen h-screen p-1 bg-transparent flex items-center justify-center">
      <HUD />
    </main>
  );
}

export default HudApp;
